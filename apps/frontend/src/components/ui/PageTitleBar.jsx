const PageTitleBar = ({ title }) => (
  <header className="flex items-center py-1">
    <h1 className="text-lg font-semibold tracking-tight text-slate-900 sm:text-[1.35rem]">{title}</h1>
  </header>
);

export default PageTitleBar;
